<%- include("head.partial.ejs") %>

<main>
  <div class="save-main">
    <h1><%= save.name %></h1>
    <div>
      <% save.data.forEach(function(d) { %>
        <p><%= Object.keys(d)[0] %>: <%= Object.values(d)[0] %></p>
      <% }) %>
      </div>

      <img class="load-save-button" src="/files/load-button.png">
    <!-- TO DO: SOMEHOW LOAD THE GAME HERE??? -->
    </div>
</main>

<%- include("foot.partial-2.ejs") %>
